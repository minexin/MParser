# v1.0 Cross-Platform Candidate Validation

Status: **accepted for the v1.0 release candidate**.

This record binds the post-contract-freeze runs that completed every
advertised release lane. They provide functional, sanitizer, SDK, package, and
performance evidence for the candidate. They are not a final 1.0.0
publication or publisher-authenticated provenance.

## Identity

- Functional/documentation candidate: revision
  `f34d8d9e00816341a01df406c2e315164886c1cc`, Actions run
  [30684969401](https://github.com/minexin/MParser/actions/runs/30684969401).
- Performance/authentication candidate: revision
  `85685b88f8f8eb4e89b03abf53aa16dbbe60c68c`, Actions run
  [30691616946](https://github.com/minexin/MParser/actions/runs/30691616946).
- Native Linux ARM64 candidate: revision
  `d69f9a829239b27f56781dd876f0e07f78e82747`, Actions run
  [30693868656](https://github.com/minexin/MParser/actions/runs/30693868656).
- Accepted native Linux ARM64 performance candidate: revision
  `3ec2bc074387283785068fe6ac98b4472a109a3a`, Actions run
  [30732814590](https://github.com/minexin/MParser/actions/runs/30732814590).
- Workflow: `Cross-platform CI`.
- Conclusion: the first two runs passed all six historical execution jobs;
  the third added native Linux ARM64, and the fourth passed all seven required
  execution jobs with the accepted ARM MIDR-labelled report pair. Release
  authentication was correctly skipped on each ordinary push.

The later run includes the tag-scoped Sigstore authentication candidate, but
that job was correctly skipped because the normal push used the default
`authenticate_release=false`. It proves workflow parsing and dependency
closure, not publisher authentication. A real release-tag dispatch remains a
separate publication gate.

## Lane Evidence

| Lane | Candidate evidence |
| --- | --- |
| Linux x64 / GCC / SLJIT | Configure, native build, full CTest suite, strict release archive/checksums, performance reports, artifact upload |
| Linux ARM64 / GCC / SLJIT native | `ubuntu-24.04-arm` host architecture check, native build, full suite, non-emulated ARM MIDR-labelled performance collection, and artifact upload passed in run `30732814590` |
| Linux x64 / Clang / ASan + UBSan | Sanitized build and full CTest suite with LeakSanitizer retained |
| Windows x64 / MSVC / SLJIT | Configure, native build, full CTest suite, strict release archive/checksums, performance reports, artifact upload |
| macOS x86-64 / Apple Clang / SLJIT | Host-architecture check, full suite, testing-disabled production SDK, relocation, independent C11/C++20 consumers, installed architecture/version checks, strict release archive, performance reports, upload |
| macOS ARM64 / Apple Clang / SLJIT | The same full native, relocated SDK, consumer, architecture/version, package, performance-report, and upload contract on an ARM64 runner |
| Linux AArch64 / GCC cross / QEMU | Native-SLJIT build and focused runtime/public-contract smokes, installed C11/C++20 consumers, strict archive plus unpacked SDK validation, artifact upload, no-SLJIT portable build/smokes/consumers, and the portable branch sample |

The native Linux ARM64 and cross/QEMU lanes are complementary. The native lane
can characterize target execution and resource behavior. The QEMU lane proves
the independent cross toolchain, native and portable target generation,
installation, package consumption, and fallback behavior; its timing does not
represent ARM performance.

The first native Linux ARM64 reports passed protocol, schema, correctness,
zero-fallback, and native-cache checks, but the hosted kernel exposed no
generic model-name field and the collector emitted `cpu_model=unknown`. Those
reports are deliberately not indexed as final hardware-labelled performance
evidence. Revision `f36a014` added raw ARM MIDR fallback and made an unknown CPU
identity a native-evidence failure. Run `30732814590` then recorded
`implementer=0x41`, architecture `8`, variant `0x0`, part `0xd49`, revision `0`
on its non-emulated aarch64 host; both accepted reports are indexed and
continuously validated.

All first-party CI builds enable warnings-as-errors. A direct audit of all
seven execution-job logs from run `30732814590` found no compiler, linker, or
CMake warning. The only broad `warning` text was Git's standard initial
branch-name hint inside checkout on non-Windows runners; it is not project
build output.

## Performance Evidence

The later run uploaded two validated reports from each native Windows x64,
Linux x64, macOS x64, and macOS ARM64 job. All eight reports were downloaded,
SHA-256 indexed, and checked into
[`baselines/v1.0/actions-30691616946`](baselines/v1.0/actions-30691616946/README.md).
They bind the exact revision, current workload hashes, Release binaries,
hardware labels, compilers, timing/resource boundaries, correctness, zero
fallback, and native-cache transitions.

Every report records `emulated=false`. The macOS ARM64 report identifies its
CPU as `Apple M1 (Virtual)`, so it is native-architecture Apple Silicon hosted
runner evidence, not a bare-metal timing claim. Linux AArch64/QEMU does not
publish a performance report. The source-tree
`cross_platform_performance_evidence_smoke` rejects report-set, manifest,
identity, source, semantic, correctness, fallback, and cache drift without
using a machine-specific timing threshold.

The final native Linux ARM64 job uploaded two additional reports. They are
SHA-256 indexed under
[`baselines/v1.0/actions-30732814590`](baselines/v1.0/actions-30732814590/README.md)
and bind revision `3ec2bc0`, GNU 13.3.0, aarch64, the raw ARM MIDR label,
`emulated=false`, exact workload/binary hashes, zero fallback, matching
results, and native-cache transitions. The independent
`linux_arm64_performance_evidence_smoke` freezes that exact two-report set.

## Release Decisions

These runs confirm:

- the frozen CLI, C ABI 1.1, header-only C++ API 1.0, machine protocol 1.0,
  builtin descriptor contract, and compatibility matrix;
- deterministic frontend/bytecode verification, invoke-many, lifecycle,
  resource, cache, allocation, unload, concurrency, and Linux sanitizer
  reliability evidence;
- installed and archived documentation, C11/C++20 consumers, CLI protocol,
  platform/architecture naming, checksum generation, and deterministic local
  provenance metadata on the advertised release matrix;
- reproducible, hardware-labelled cross-platform performance/resource
  evidence for the v1.0 target set.

Accordingly, `G-RELIABILITY-001`, `G-PERFORMANCE-001`, and
`G-DOCUMENTATION-001` are closed. The candidate remains version 0.90.0 and the
public contract remains `frozen-v1-candidate`; final versioning, tag, and
publication are separate operations.

## Remaining Boundaries

The candidate still does not close:

- `G-PROVENANCE-001`: uploaded local statements are deterministic unsigned
  audit metadata. The default-off Sigstore candidate can validate and sign
  same-run release-tag artifacts, but no real tag-scoped signing run has
  executed yet;
- `G-SANITIZER-001`: Linux TSan and macOS ARM64 sanitizer feasibility remain
  focused evaluations, while local Windows MSVC ASan is evidence rather than
  a recurring lane;
- final `1.0.0` artifacts, tag, release-page publication, and authenticated
  provenance.

The release-candidate readiness test freezes the remaining Must-have set to
`G-PROVENANCE-001`, keeps `G-SANITIZER-001` as a bounded Should-have, and keeps
broader JIT coverage deferred to v1.x.
