# v1.0 Release Documentation Gate

This gate turns the existing architecture and public-contract records into a
coherent release-facing manual set. It does not create a second semantic
authority: the compatibility matrix, CLI contract, public contract manifest,
protocol schema, public headers, and builtin catalog remain authoritative.

Status: **closed for the v1.0 candidate**.

## Published Manual Set

The task-oriented [documentation index](README.md) routes:

- script users to the [User Manual](user-manual.md),
  [CLI Reference](cli-reference.md), and
  [Support Matrix](support-matrix.md);
- builders to [Build And Install](build-and-install.md);
- runtime operators to [JIT And Fallback](jit-and-fallback.md) and
  [Runtime Boundaries](runtime-boundaries.md);
- C and C++ hosts to the embedding and ABI guides;
- builtin authors to the source-extension contract;
- pre-v1 users to [Migrating From v0.x To v1.0](migration-v1.0.md);
- release operators to the [v1 Release Process](release-process.md);
- contract reviewers to versioning, freeze, release notes, v1.0/v1.x
  roadmaps, reliability, performance evidence, and the measured
  [v1.0 JIT scope decision](v1.0-jit-scope-decision.md).

Every scope guide states that v1.0 is a MATLAB-like subset runtime rather than
complete MATLAB compatibility. The support summary distinguishes parser,
production VM, portable typed, and native states and names explicit
unsupported/Post-v1.0 work.

## Executable Consistency

`release_documentation_smoke`:

1. requires the complete release manual set and rejects empty files or
   unfinished markers;
2. verifies that the documentation index and top-level README route every
   required guide;
3. reads `cli-contract-v1.json` with CMake's JSON parser;
4. requires every frozen production mode/option, diagnostic mode, backend
   selector, utility command, and removed-mode migration in
   `cli-reference.md`;
5. checks the same option names against the built CLI's live `--help`;
6. executes the documented human production command with `--jit=off` and
   verifies `summary = 705`;
7. executes the documented JSON command and parses a successful
   `mparser.result` major-1 document with empty stderr.

The check derives its option list from the frozen contract rather than
maintaining a second hand-written list in the test.

## Installed And Archived SDK

CMake installs the manual set beside the versioned schemas, manifests,
license, notices, and builtin catalog. The source `docs/README.md` is installed
as `documentation-index.md` so it does not overwrite the project overview
`README.md`.

Both relocated installed-consumer tests require all release guides before
building and running independent C11 and multi-translation-unit C++20 hosts.
`release_archive_smoke` requires the same files after unpacking, then runs
those consumers and the installed JSON CLI without source-tree access.

The release archive and unsigned SLSA Provenance v1 statement are generated
twice from one fixed compiled payload with normalized metadata and must have
identical hashes. The statement binds source revision, public contract
inputs, build parameters, toolchain/platform, builder boundary, and archive
digest. Negative gates require modified archive content and statement
authentication metadata to be rejected. Its `.sha256` sidecar and the
two-entry `SHA256SUMS` are authoritative
because an archive hash cannot be embedded inside that same archive without
changing it.

## Windows Candidate Evidence

The current documentation candidate passed:

- Windows x64 MSVC Release native: 205/205 tests;
- Windows x64 MSVC Release no-JIT: 198/198 tests;
- focused native and no-JIT documentation, compatibility-matrix, installed C,
  and installed C++ gates;
- packaging-tree `release` label: 4/4 documentation, readiness, negative
  guard, and reproducible relocated archive gates;
- a regenerated `mparser-0.90.0-windows-x86_64.zip` with matching SHA-256
  sidecar, deterministic unsigned provenance statement, and two-entry
  `SHA256SUMS`.

The first direct installed-consumer attempt outside a Visual Studio developer
environment could not locate `kernel32.lib`. Re-running under `vcvars64`
passed; this confirms the build-guide requirement to activate the actual MSVC
toolchain rather than treating a plain shell as a compiler verdict.

## Cross-Platform Closure

Revision `f34d8d9e00816341a01df406c2e315164886c1cc` passed every lane in
[Actions run 30684969401](https://github.com/minexin/MParser/actions/runs/30684969401).
The run repeated full or focused public-contract tests as appropriate, created
strict clean-source archives on every release target, exercised relocated or
unpacked C11/C++20 SDK consumers, checked installed CLIs and architecture, and
uploaded the candidate artifacts. Linux AArch64 additionally exercised both
native SLJIT and no-SLJIT portable execution under QEMU.

The exact scope and exclusions are recorded in
[v1.0-cross-platform-validation.md](v1.0-cross-platform-validation.md).
`G-DOCUMENTATION-001` is therefore closed; `DOC-001` remains the executable
manual/package contract.

## Next Gate

The local Windows MSVC ASan no-JIT branch of specialist sanitizer evaluation
is now closed and recorded in [v1.0-reliability.md](v1.0-reliability.md).
The local provenance builder/validator, release-process specification,
candidate release notes, v1.x roadmap, and machine-checked candidate-readiness
gate are also implemented. That gate permits no open Must-haves and rejects
any new framework-impacting blocker; negative fixtures
also prove blocker-set changes and unreviewed reactivation of the deferred
v1.x JIT breadth item cannot pass silently. Cross-platform candidate packaging
is complete. The default-off manual-tag Sigstore candidate, package-input
validator, public-transparency disclosure, and consumer verification policy
are documented in [release-authentication.md](release-authentication.md).
Performance characterization and the real `v0.90.1` authenticated tag run are
closed with immutable evidence; Linux TSan and macOS sanitizer remain bounded
Should-have evaluations. The final freeze then sets version 1.0.0, refreshes
matrix/contract snapshots, and publishes the checksummed authenticated
artifacts.
